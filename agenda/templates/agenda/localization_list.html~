{% extends "agenda/base.html" %}
 
{% block content %}

	<a class="btn btn-default" href="{%url 'agenda:localization_create'%}" role="button">Crear nueva localización</a>

{% if localization_list %}

		<h3>Localizaciones</h3>

	<div class="table-responsive">
		<table class="table">
			<tr>
				<td><h4>Nombre</h4></td>
				<td><h4>Teléfono</h4></td>
				<td><h4>Dirección</h4></td>
				<td><h4>Email</h4></td>
				<td><h4>Opciones</h4></td>
			</tr>
			{% for localization in localization_list %}
				<tr>
		    		<td>
		    			<h5><a href="{%url 'agenda:localization_detail' localization.id%}">{{localization.name}}</a>
					</h5>
		    		</td>
		    		<td>
		    			<h5>{{localization.phone1}}</h5>
		    		</td>
		    		<td>
		    			<h5>{{localization.address1}}</h5>
		    		</td>
		    		<td>
		    			<h5>{{localization.email1}}</h5>
		    		</td>
		    		<td align="center">
		    			<form action="{%url 'agenda:localization_delete' localization.id%}" method="post">
		    				{% csrf_token %}
		    				<button type="submit" class="btn btn-default">Eliminar</button>
		    			</form>
		    		</td>
		    	</tr>
		    {% endfor %}
		</table>
	</div>
{% else %}
<div align="center">
    <h2>No hay localizaciones disponibles</h2>
</div>
{% endif %}
<a class="btn btn-primary" href="{%url 'agenda:contact_list'%}" role="button">Volver a Contactos</a>
{% endblock %}
