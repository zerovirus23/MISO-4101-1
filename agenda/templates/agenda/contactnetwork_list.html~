{% extends "agenda/base.html" %}
 
{% block content %}

<div align="center">
	<a class="btn btn-default"" href="{%url 'agenda:network_create'%}" role="button">Crear nueva red</a>
</div>
{% if network_list %}


		<h2>Redes Sociales</h2>

	
		<table class="table table-condensed table-responsive">
			<tr>
				<td>Tipo Red</td>
				<td>Usuario</td>
				<td>Opciones</td>
			</tr>
			{% for network in network_list %}
				<tr>
		    		<td>
		    			<h4><a href="{%url 'agenda:network_detail' network.id%}">{{network.name}}</a>
					</h4>
		    		</td>
		    		<td>
		    			{{network.username}}
		    		</td>
		    		<td align="center">
		    			<form action="{%url 'agenda:network_delete' network.id%}" method="post">
		    				{% csrf_token %}
		    				<button class="btn btn-default" type="submit">Eliminar</button>
		    			</form>
		    		</td>
		    	</tr>
		    {% endfor %}
		</table>
	

{% else %}
<div align="center">
    <h2>No hay redes disponibles</h2>
</div>
{% endif %}
<a href="{%url 'agenda:contact_list'%}">Volver a Contactos</a>
{% endblock %}
